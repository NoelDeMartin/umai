DELETE DATA {
    @prefix schema: <https://schema.org/> .

	<#it>
        schema:name "Ramen" ;
        schema:recipeIngredient "Broth" .

    <#instruction-step-2> schema:text "Step 2" .
} ;

INSERT DATA {
    @prefix schema: <https://schema.org/> .

    <#it>
        schema:name "Ramen!" ;
        schema:recipeIngredient "Shiitake" .

    <#instruction-step-2> schema:text "Step two" .
}
